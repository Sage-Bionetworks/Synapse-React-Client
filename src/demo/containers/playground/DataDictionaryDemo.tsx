import React, { ReactElement } from 'react'
import DataDictionary from 'lib/containers/data_dictionary'

export default function DataDictionaryDemo(): ReactElement {
  return <DataDictionary title={`Data Schema`} url={``} />
}
