import React, { ReactElement } from 'react'
import DataSchema from 'lib/containers/data_schema'

export default function DataSchemaDemo(): ReactElement {
  return <DataSchema title={`Data Schema`} url={``} />
}
